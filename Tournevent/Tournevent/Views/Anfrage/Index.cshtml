@model IEnumerable < Tournevent.Models.AnfrageDaten >

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@Styles.Render("~/Content/Site.css")

<h2>Anfrage</h2>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VereinsName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Vorname)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nachname)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>
        <th>Aktionen</th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.VereinsName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Vorname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nachname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td class="tableActions">
                @Html.ActionLink("Annehmen", "Accept", new { id = item.userId }) |
                @Html.ActionLink("Ablehnen", "Reject", new { id = item.userId })
            </td>
        </tr>
    }

</table>
